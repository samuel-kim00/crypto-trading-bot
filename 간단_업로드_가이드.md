# 🚀 GitHub 업로드 간단 가이드

## 3단계로 끝내기!

### 1️⃣ GitHub에 저장소 만들기
1. https://github.com 접속
2. 로그인
3. 우측 상단 **"+"** 버튼 클릭
4. **"New repository"** 선택
5. 저장소 이름 입력 (예: `crypto-trading-bot`)
6. **Public** 또는 **Private** 선택
7. **"Create repository"** 클릭
   - ⚠️ **"Initialize with README" 체크하지 마세요!**

### 2️⃣ 터미널에서 명령어 실행

```bash
# 모든 파일 추가
git add .

# 커밋 생성
git commit -m "Initial commit: Trading bot"

# GitHub 연결 (아래 URL을 본인의 저장소 URL로 변경!)
git remote add origin https://github.com/당신의사용자명/저장소명.git

# 브랜치 이름 변경
git branch -M main

# 업로드!
git push -u origin main
```

### 3️⃣ 완료! 🎉

GitHub 페이지에서 파일들이 보이는지 확인하세요!

---

## 💡 저장소 URL 찾는 방법

GitHub에서 저장소를 만든 후 나오는 페이지에서:
- **HTTPS** 버튼 클릭
- URL 복사 (예: `https://github.com/username/repo.git`)
- 위 명령어의 `https://github.com/당신의사용자명/저장소명.git` 부분에 붙여넣기

---

## ❓ 문제가 생기면?

### "remote origin already exists" 에러
```bash
git remote remove origin
git remote add origin https://github.com/당신의사용자명/저장소명.git
```

### 인증 문제
- GitHub → Settings → Developer settings → Personal access tokens
- 새 토큰 생성 후 비밀번호 대신 사용

---

**이제 바로 시작하세요!** 🚀

